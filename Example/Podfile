use_frameworks!

target 'Form_Example' do
  pod "Pastel"
  pod 'Form', :path => '../'

  target 'Form_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
    pod 'Nimble', '~> 5.1.1'
    pod 'FBSnapshotTestCase'
    pod 'Nimble-Snapshots'
  end
end
